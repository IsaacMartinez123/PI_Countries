import SearchBar from './SearchBar';

const Navbar = () => {
    return (
        <nav>
            <ul>
                <li>
                    <SearchBar />
                </li>
            </ul>
        </nav>
    );
};

export default Navbar;
